// Caution: impossible to change this value for the moment the
// apollo-server-micro does not seem to take into account another path than
// `/graphql` for the handling of websockets.
export const GRAPHQL_PATH = '/graphql'
